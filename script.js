// You can add JavaScript functionality if needed, such as loading more posts, filtering by categories, etc.
// Currently, this file is empty, but you can expand it later.

document.addEventListener('DOMContentLoaded', function () {
    console.log("Finance Blog Loaded!");
});
